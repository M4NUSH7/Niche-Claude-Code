# Local dev only (wrangler dev --local reads this via .dev.vars, not .env)
# Real secrets go through: wrangler secret put OAUTH_CLIENT_SECRET
OAUTH_ISSUER=https://your-idp.example.com
JWKS_URI=https://your-idp.example.com/.well-known/jwks.json
