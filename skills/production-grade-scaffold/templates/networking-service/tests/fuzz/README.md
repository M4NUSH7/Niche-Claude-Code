Put fuzz targets here against the protocol/ parser (cargo-fuzz / AFL / go-fuzz depending on
language). Wire a short time-boxed run into CI and a longer scheduled run separately.
See references/architecture-archetypes.md #4.
