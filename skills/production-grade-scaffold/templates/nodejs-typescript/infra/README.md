# Infrastructure as Code

Put Terraform/Pulumi modules here (declarative provisioning), kept separate from any
Kubernetes/Ansible configuration-management manifests. Remote state encrypted + locked,
Checkov/TFLint in CI, no manual console changes to live infra. See references/infrastructure-delivery.md.
