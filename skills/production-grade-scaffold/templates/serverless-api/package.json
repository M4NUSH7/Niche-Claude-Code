{
  "name": "production-grade-serverless-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "deploy": "serverless deploy"
  },
  "dependencies": {
    "zod": "^3.23.0",
    "jose": "^5.6.0",
    "@neondatabase/serverless": "^0.9.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^1.6.0",
    "serverless": "^3.39.0",
    "@types/aws-lambda": "^8.10.0"
  }
}
